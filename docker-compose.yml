version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: loteria_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: loteria_analytics
    ports:
      - "5432:5432"
    volumes:
      - loteria_pg_data:/var/lib/postgresql/data

volumes:
  loteria_pg_data:
